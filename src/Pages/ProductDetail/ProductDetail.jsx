import React from 'react';
import Layout from '../../Components/Layout/Layout';

const ProductDetail = () => {
    return (
        <Layout>
            product detail
        </Layout>
    );
}

export default ProductDetail;
